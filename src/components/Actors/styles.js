import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
  image: {
    maxWidth: '80%',
    boxShadow: '0.5em 1em 1em rgb(64, 64, 70)',
    objectFit: 'cover',
    borderRadius: '20px',
  },
}));
